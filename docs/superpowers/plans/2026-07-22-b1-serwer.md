# B1 — Serwer czatu agentów: plan implementacji

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Serwer WS z pokojem, tożsamością instance/generation, kolejką tasków (lease+CAS), trwałym room_seq z resync oraz activation envelope — wg speca `docs/superpowers/specs/2026-07-22-statek-matka-krok-b-design.md` (commit cfd9577).

**Architecture:** Pakiet `chat/` z czterema modułami o czystych granicach: `protocol` (ramki/wzmianki/envelope, zero I/O), `store` (trwały log zdarzeń + snapshot), `identity` (nick/instance/generation/tokeny), `tasks` (kolejka: stany, lease, CAS, dedup) — spinane przez `chat/server.py` (jedyny moduł z asyncio/websockets). Logika testowana synchronicznie bez sieci; sieć tylko w testach integracyjnych.

**Tech Stack:** Python 3.12+, `websockets` 10.4 (systemowy), poza tym stdlib. Testy: pytest przez `uv run --with pytest --with websockets`.

## Global Constraints

- Spec = `docs/superpowers/specs/2026-07-22-statek-matka-krok-b-design.md`; bramka akceptacji B1 z sekcji "Etapy implementacji" jest wiążąca.
- Tylko stdlib + websockets 10.4; ŻADNYCH nowych zależności produkcyjnych.
- Port i ścieżki danych z env: `CHAT_PORT` (default 8765), `CHAT_DATA` (default `./chat-data`).
- Czas zawsze wstrzykiwany jako argument `now: float` (testy deterministyczne, bez sleep w logice).
- Stary PoC (`server.py`, `send.py` w korzeniu) zostaje nietknięty do Zadania 7.
- Testy odpalane: `uv run --quiet --with pytest --with websockets python -m pytest <plik> -v`.
- Commit po każdym zadaniu, prefix `b1:`.

## Struktura plików (docelowa)

```
chat/__init__.py      — puste
chat/protocol.py      — wzmianki, walidacja ramek, activation envelope
chat/store.py         — EventLog: room_seq, JSONL, snapshot, retencja, resync
chat/identity.py      — Registry: tokeny, instance_id, generation
chat/tasks.py         — TaskQueue: stany, CAS, dedup, lease, WIP, dump/restore
chat/server.py        — wiring asyncio/websockets (hello, routing, offer)
tests/test_protocol.py
tests/test_store.py
tests/test_identity.py
tests/test_tasks.py
tests/test_server_integration.py
send.py               — (Zadanie 7) hello + plik sesji instance_id
test_chat.py          — (Zadanie 7) aktualizacja do nowej semantyki echa
```

---

### Task 1: `chat/protocol.py` — wzmianki, walidacja, envelope

**Files:**
- Create: `chat/__init__.py` (pusty), `chat/protocol.py`
- Test: `tests/test_protocol.py`, `tests/__init__.py` (pusty)

**Interfaces:**
- Produces: `parse_mentions(text: str) -> set[str]`; `validate(frame: dict) -> str | None`; `make_frame(ftype: str, frm: str, ts: float, **fields) -> dict`; `make_envelope(nick: str, frames: list[dict], seq_from: int, seq_to: int) -> dict` z deterministycznym `activation_id == f"{nick}:{seq_from}-{seq_to}"`.

- [ ] **Step 1: Napisz failing testy**

```python
# tests/test_protocol.py
from chat import protocol


def test_parse_mentions():
    assert protocol.parse_mentions("hej @alfa i @beta, reszta nie") == {"alfa", "beta"}
    assert protocol.parse_mentions("@all ruszamy") == {"all"}
    assert protocol.parse_mentions("bez wzmianek") == set()
    assert protocol.parse_mentions("mail x@y.z to nie wzmianka") == set()


def test_make_frame_and_validate_ok():
    f = protocol.make_frame("chat", "alfa", ts=123.0, text="siema")
    assert f == {"type": "chat", "from": "alfa", "ts": 123.0, "text": "siema"}
    assert protocol.validate(f) is None


def test_validate_rejects():
    assert protocol.validate({"from": "x", "ts": 1.0}) == "missing type"
    assert protocol.validate({"type": "nope", "from": "x", "ts": 1.0}) == "unknown type: nope"
    assert protocol.validate({"type": "chat", "ts": 1.0}) == "missing from"
    assert protocol.validate({"type": "chat", "from": "x"}) == "missing ts"


def test_envelope_deterministic_activation_id():
    frames = [protocol.make_frame("chat", "beta", ts=1.0, text="@alfa hej")]
    e1 = protocol.make_envelope("alfa", frames, seq_from=5, seq_to=9)
    e2 = protocol.make_envelope("alfa", frames, seq_from=5, seq_to=9)
    assert e1["activation_id"] == e2["activation_id"] == "alfa:5-9"
    assert e1["backlog"] == frames
    assert e1["seq_from"] == 5 and e1["seq_to"] == 9
```

- [ ] **Step 2: Uruchom — ma polec**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_protocol.py -v`
Expected: FAIL (`ModuleNotFoundError: chat`)

- [ ] **Step 3: Implementacja**

```python
# chat/protocol.py
"""Ramki, wzmianki i activation envelope. Zero I/O."""
import re

FRAME_TYPES = {
    "hello", "chat", "fyi", "status", "backlog", "resync_required",
    "task_new", "task_offer", "task_claim", "task_done", "task_blocked",
    "review_changes", "error", "ok",
}
_MENTION = re.compile(r"(?:^|\s)@(\w+)")


def parse_mentions(text):
    return set(_MENTION.findall(text or ""))


def make_frame(ftype, frm, ts, **fields):
    return {"type": ftype, "from": frm, "ts": ts, **fields}


def validate(frame):
    if "type" not in frame:
        return "missing type"
    if frame["type"] not in FRAME_TYPES:
        return f"unknown type: {frame['type']}"
    if "from" not in frame:
        return "missing from"
    if "ts" not in frame:
        return "missing ts"
    return None


def make_envelope(nick, frames, seq_from, seq_to):
    return {
        "activation_id": f"{nick}:{seq_from}-{seq_to}",
        "nick": nick,
        "seq_from": seq_from,
        "seq_to": seq_to,
        "backlog": frames,
    }
```

- [ ] **Step 4: Testy zielone**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_protocol.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add chat/ tests/
git commit -m "b1: protocol — wzmianki, walidacja ramek, activation envelope"
```

---

### Task 2: `chat/store.py` — EventLog z room_seq, snapshotem i resync

**Files:**
- Create: `chat/store.py`
- Test: `tests/test_store.py`

**Interfaces:**
- Consumes: nic (stdlib).
- Produces: `EventLog(dirpath: Path, retention: int = 500)` z: `append(frame: dict) -> int` (nadaje seq, trwały zapis PRZED zwrotem), `last_seq: int`, `snapshot_seq: int`, `events_after(seq: int) -> list[dict] | None` (**None = resync_required**, gdy seq < snapshot_seq), `save_snapshot(state: dict) -> None` (atomowo: tmp+rename, POTEM kompakcja eventów ≤ snapshot_seq), `load_snapshot() -> tuple[dict, int] | None`, `replay() -> list[dict]` (eventy > snapshot_seq po restarcie).

- [ ] **Step 1: Napisz failing testy**

```python
# tests/test_store.py
from chat.store import EventLog


def test_append_assigns_monotonic_seq(tmp_path):
    log = EventLog(tmp_path)
    assert log.append({"type": "chat", "text": "a"}) == 1
    assert log.append({"type": "chat", "text": "b"}) == 2
    assert log.last_seq == 2


def test_events_after(tmp_path):
    log = EventLog(tmp_path)
    for t in "abc":
        log.append({"type": "chat", "text": t})
    got = log.events_after(1)
    assert [e["text"] for e in got] == ["b", "c"]
    assert [e["seq"] for e in got] == [2, 3]
    assert log.events_after(3) == []


def test_survives_restart(tmp_path):
    log = EventLog(tmp_path)
    log.append({"type": "chat", "text": "a"})
    log2 = EventLog(tmp_path)  # nowy obiekt, ten sam katalog
    assert log2.last_seq == 1
    assert log2.events_after(0)[0]["text"] == "a"


def test_snapshot_then_old_cursor_requires_resync(tmp_path):
    log = EventLog(tmp_path)
    for i in range(5):
        log.append({"type": "chat", "text": str(i)})
    log.save_snapshot({"queue": "stan"})
    assert log.snapshot_seq == 5
    log.append({"type": "chat", "text": "po-snapshocie"})
    # kursor sprzed snapshotu -> jawny resync, nigdy partial replay
    assert log.events_after(3) is None
    # kursor na snapshocie -> zwykly replay nowszych
    assert [e["text"] for e in log.events_after(5)] == ["po-snapshocie"]
    state, seq = log.load_snapshot()
    assert state == {"queue": "stan"} and seq == 5


def test_snapshot_persists_and_replay_after_restart(tmp_path):
    log = EventLog(tmp_path)
    for i in range(3):
        log.append({"type": "chat", "text": str(i)})
    log.save_snapshot({"s": 1})
    log.append({"type": "chat", "text": "x"})
    log2 = EventLog(tmp_path)
    assert log2.snapshot_seq == 3 and log2.last_seq == 4
    assert [e["text"] for e in log2.replay()] == ["x"]
```

- [ ] **Step 2: Uruchom — ma polec**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_store.py -v`
Expected: FAIL (`ImportError`)

- [ ] **Step 3: Implementacja**

```python
# chat/store.py
"""Trwaly log zdarzen pokoju: room_seq, snapshot, retencja.

Kolejnosc trwalosci (spec): przy mutacji najpierw trwaly event, potem
publikacja; przy kompakcji najpierw snapshot (tmp+rename), potem usuniecie
przykrytych eventow. events_after zwraca None gdy kursor wypada przed
snapshot_seq — jawny resync_required, nigdy cichy partial replay.
"""
import json
import os
from pathlib import Path


class EventLog:
    def __init__(self, dirpath, retention=500):
        self.dir = Path(dirpath)
        self.dir.mkdir(parents=True, exist_ok=True)
        self.events_path = self.dir / "events.jsonl"
        self.snapshot_path = self.dir / "snapshot.json"
        self.retention = retention
        self.snapshot_seq = 0
        self._events = []  # [{seq, ...frame}] tylko > snapshot_seq
        if self.snapshot_path.exists():
            data = json.loads(self.snapshot_path.read_text())
            self.snapshot_seq = data["snapshot_seq"]
        self.last_seq = self.snapshot_seq
        if self.events_path.exists():
            with self.events_path.open() as f:
                for line in f:
                    e = json.loads(line)
                    if e["seq"] > self.snapshot_seq:
                        self._events.append(e)
                        self.last_seq = e["seq"]

    def append(self, frame):
        self.last_seq += 1
        event = {"seq": self.last_seq, **frame}
        with self.events_path.open("a") as f:
            f.write(json.dumps(event) + "\n")
            f.flush()
            os.fsync(f.fileno())
        self._events.append(event)
        return self.last_seq

    def events_after(self, seq):
        if seq < self.snapshot_seq:
            return None  # resync_required
        return [e for e in self._events if e["seq"] > seq]

    def save_snapshot(self, state):
        seq = self.last_seq
        tmp = self.dir / "snapshot.json.tmp"
        tmp.write_text(json.dumps({"snapshot_seq": seq, "state": state}))
        tmp.rename(self.snapshot_path)  # atomowo; dopiero teraz kompakcja
        self.snapshot_seq = seq
        self._events = [e for e in self._events if e["seq"] > seq]
        with self.events_path.open("w") as f:
            for e in self._events:
                f.write(json.dumps(e) + "\n")

    def load_snapshot(self):
        if not self.snapshot_path.exists():
            return None
        data = json.loads(self.snapshot_path.read_text())
        return data["state"], data["snapshot_seq"]

    def replay(self):
        return list(self._events)
```

- [ ] **Step 4: Testy zielone**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_store.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add chat/store.py tests/test_store.py
git commit -m "b1: store — EventLog z room_seq, snapshotem i jawnym resync"
```

---

### Task 3: `chat/identity.py` — tokeny, instance_id, generation

**Files:**
- Create: `chat/identity.py`
- Test: `tests/test_identity.py`

**Interfaces:**
- Produces: `Registry(tokens: dict[str, str])` z: `hello(nick, instance_id, token) -> int` (zwraca generation; podbija TYLKO przy nowym instance_id; zły token → `AuthError`), `is_current(nick, generation) -> bool`, `generation_of(nick) -> int`.
- Wyjątek: `AuthError(Exception)`.

- [ ] **Step 1: Napisz failing testy**

```python
# tests/test_identity.py
import pytest
from chat.identity import Registry, AuthError

TOKENS = {"alfa": "tok-a", "beta": "tok-b"}


def test_hello_issues_generation_and_auth():
    r = Registry(TOKENS)
    assert r.hello("alfa", "inst-1", "tok-a") == 1
    with pytest.raises(AuthError):
        r.hello("alfa", "inst-1", "zly-token")
    with pytest.raises(AuthError):
        r.hello("nieznany", "inst-1", "cokolwiek")


def test_reconnect_same_instance_keeps_generation():
    r = Registry(TOKENS)
    g1 = r.hello("alfa", "inst-1", "tok-a")
    g2 = r.hello("alfa", "inst-1", "tok-a")  # reconnect / drugi socket
    assert g1 == g2 == 1
    assert r.is_current("alfa", g1)


def test_takeover_bumps_generation_and_invalidates_old():
    r = Registry(TOKENS)
    g1 = r.hello("alfa", "inst-1", "tok-a")
    g2 = r.hello("alfa", "inst-2", "tok-a")  # przejecie nicku
    assert g2 == g1 + 1
    assert not r.is_current("alfa", g1)
    assert r.is_current("alfa", g2)
```

- [ ] **Step 2: Uruchom — ma polec**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_identity.py -v`
Expected: FAIL (`ImportError`)

- [ ] **Step 3: Implementacja**

```python
# chat/identity.py
"""Tozsamosc logiczna: nick + instance_id -> generation. Token per agent."""


class AuthError(Exception):
    pass


class Registry:
    def __init__(self, tokens):
        self.tokens = tokens          # nick -> token
        self._gen = {}                # nick -> int
        self._instance = {}           # nick -> aktualny instance_id

    def hello(self, nick, instance_id, token):
        if self.tokens.get(nick) != token:
            raise AuthError(f"bad token for {nick}")
        if self._instance.get(nick) != instance_id:
            self._gen[nick] = self._gen.get(nick, 0) + 1
            self._instance[nick] = instance_id
        return self._gen[nick]

    def generation_of(self, nick):
        return self._gen.get(nick, 0)

    def is_current(self, nick, generation):
        return self._gen.get(nick) == generation
```

- [ ] **Step 4: Testy zielone**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_identity.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add chat/identity.py tests/test_identity.py
git commit -m "b1: identity — Registry z instance_id/generation i tokenami"
```

---

### Task 4: `chat/tasks.py` cz. 1 — add/claim, CAS, dedup, stale generation

**Files:**
- Create: `chat/tasks.py`
- Test: `tests/test_tasks.py`

**Interfaces:**
- Produces: wyjątki `TaskError`, `Conflict(TaskError)`, `StaleGeneration(TaskError)`; `TaskQueue(wip_limit=3, lease_ttl=120.0, dedup_ttl=3600.0)` z: `add(card: dict, command_id: str, now: float) -> dict`, `claim(task_id, nick, generation, command_id, expected_version, now) -> dict`, `get(task_id) -> dict`. Task = dict: `{id, card, status, assignee, generation, version, lease_until, frozen}`. Dedup: powtórzone `command_id` zwraca ZAPAMIĘTANY wynik, bez mutacji. CAS: `expected_version != task["version"]` → `Conflict` bez mutacji. Mutacja taska z `generation != task["generation"]` → `StaleGeneration` (dla claim: generation zapisywana, nie sprawdzana).
- Consumes: nic (czysta logika).

- [ ] **Step 1: Napisz failing testy**

```python
# tests/test_tasks.py
import pytest
from chat.tasks import TaskQueue, Conflict, StaleGeneration

CARD = {"goal": "napisz foo", "acceptance": "test przechodzi",
        "verify": "pytest -k foo", "files": ["foo.py"],
        "head": "abc123", "brief": "def456"}


def make_claimed(q, now=0.0):
    t = q.add(CARD, command_id="c-add", now=now)
    return q.claim(t["id"], "beta", generation=1, command_id="c-claim",
                   expected_version=t["version"], now=now)


def test_add_and_claim():
    q = TaskQueue()
    t = q.add(CARD, command_id="c1", now=0.0)
    assert t["status"] == "open" and t["version"] == 1 and t["card"] == CARD
    c = q.claim(t["id"], "beta", generation=1, command_id="c2",
                expected_version=1, now=0.0)
    assert c["status"] == "claimed" and c["assignee"] == "beta"
    assert c["version"] == 2 and c["lease_until"] > 0.0


def test_command_id_dedup_returns_same_result_no_double_mutation():
    q = TaskQueue()
    t = q.add(CARD, command_id="c1", now=0.0)
    again = q.add(CARD, command_id="c1", now=1.0)  # duplikat komendy
    assert again["id"] == t["id"]
    c1 = q.claim(t["id"], "beta", 1, "c2", expected_version=1, now=0.0)
    c2 = q.claim(t["id"], "beta", 1, "c2", expected_version=1, now=0.0)
    assert c1 == c2 and q.get(t["id"])["version"] == 2  # jedna mutacja


def test_cas_conflict_without_mutation():
    q = TaskQueue()
    t = q.add(CARD, command_id="c1", now=0.0)
    with pytest.raises(Conflict):
        q.claim(t["id"], "beta", 1, "c2", expected_version=99, now=0.0)
    assert q.get(t["id"])["status"] == "open"
    assert q.get(t["id"])["version"] == 1


def test_stale_generation_rejected():
    q = TaskQueue()
    c = make_claimed(q)
    with pytest.raises(StaleGeneration):
        q.heartbeat(c["id"], "beta", generation=0, now=1.0)  # stara sesja
```

(`test_stale_generation_rejected` używa `heartbeat` — jego minimalna wersja wchodzi w tym zadaniu, pełna mechanika lease w Zadaniu 5.)

- [ ] **Step 2: Uruchom — ma polec**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_tasks.py -v`
Expected: FAIL (`ImportError`)

- [ ] **Step 3: Implementacja**

```python
# chat/tasks.py
"""Kolejka taskow: stany, lease z TTL, CAS, dedup po command_id.

Stany: open -> claimed -> review -> done; odnogi: changes_requested
(-> claimed, ten sam assignee, lease zachowany) i blocked (TTL zamrozony,
poza WIP). Zamrazanie egzekwuje serwer/kolejka, nie klient.
"""
import copy


class TaskError(Exception):
    pass


class Conflict(TaskError):
    pass


class StaleGeneration(TaskError):
    pass


class TaskQueue:
    def __init__(self, wip_limit=3, lease_ttl=120.0, dedup_ttl=3600.0):
        self.wip_limit = wip_limit
        self.lease_ttl = lease_ttl
        self.dedup_ttl = dedup_ttl
        self._tasks = {}      # id -> task dict
        self._results = {}    # command_id -> (expires_at, deepcopy wyniku)
        self._next_id = 0

    # -- dedup ------------------------------------------------------------
    def _dedup_get(self, command_id, now):
        hit = self._results.get(command_id)
        if hit and hit[0] > now:
            return copy.deepcopy(hit[1])
        return None

    def _dedup_put(self, command_id, result, now):
        self._results[command_id] = (now + self.dedup_ttl, copy.deepcopy(result))
        return result

    # -- operacje ----------------------------------------------------------
    def add(self, card, command_id, now):
        cached = self._dedup_get(command_id, now)
        if cached is not None:
            return cached
        self._next_id += 1
        task = {"id": f"t{self._next_id}", "card": card, "status": "open",
                "assignee": None, "generation": None, "version": 1,
                "lease_until": None, "frozen": False}
        self._tasks[task["id"]] = task
        return self._dedup_put(command_id, copy.deepcopy(task), now)

    def get(self, task_id):
        return copy.deepcopy(self._tasks[task_id])

    def _check(self, task, expected_version):
        if task["version"] != expected_version:
            raise Conflict(
                f"{task['id']}: version {task['version']} != {expected_version}")

    def _check_owner(self, task, nick, generation):
        if task["assignee"] != nick or task["generation"] != generation:
            raise StaleGeneration(
                f"{task['id']}: {nick}/gen{generation} nie jest wlascicielem")

    def claim(self, task_id, nick, generation, command_id, expected_version, now):
        cached = self._dedup_get(command_id, now)
        if cached is not None:
            return cached
        task = self._tasks[task_id]
        self._check(task, expected_version)
        if task["status"] != "open":
            raise Conflict(f"{task_id}: status {task['status']}, nie open")
        task.update(status="claimed", assignee=nick, generation=generation,
                    version=task["version"] + 1,
                    lease_until=now + self.lease_ttl)
        return self._dedup_put(command_id, copy.deepcopy(task), now)

    def heartbeat(self, task_id, nick, generation, now):
        task = self._tasks[task_id]
        self._check_owner(task, nick, generation)
        if not task["frozen"]:
            task["lease_until"] = now + self.lease_ttl
```

- [ ] **Step 4: Testy zielone**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_tasks.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add chat/tasks.py tests/test_tasks.py
git commit -m "b1: tasks cz.1 — add/claim z CAS, dedup command_id, stale generation"
```

---

### Task 5: `chat/tasks.py` cz. 2 — lease/expiry, blocked-freeze, review-cykl, WIP, dump

**Files:**
- Modify: `chat/tasks.py` (dopisz metody do `TaskQueue`)
- Test: `tests/test_tasks.py` (dopisz testy)

**Interfaces:**
- Produces (dopisane do `TaskQueue`): `block(task_id, nick, generation, command_id, expected_version, now)` (→ `blocked`, `frozen=True`), `unblock(...)` (→ `claimed`, odmraża, świeży lease), `to_review(...)` (→ `review`, `frozen=True` — w review lease nie tyka), `request_changes(task_id, command_id, expected_version, now)` (→ `claimed`, TEN SAM assignee/generation, odmrożony świeży lease), `done(task_id, nick, generation, command_id, expected_version, now)` (→ `done`), `expire(now) -> list[dict]` (wygasłe → `open`, czyści assignee; zwraca każdy task RAZ), `offerable() -> dict | None` (najstarszy `open`, o ile liczba claimed+review < wip_limit), `dump() -> dict` / `TaskQueue.restore(data, **kwargs) -> TaskQueue`.

- [ ] **Step 1: Dopisz failing testy**

```python
# tests/test_tasks.py — DOPISZ na koncu
def test_lease_expiry_reopens_exactly_once():
    q = TaskQueue(lease_ttl=10.0)
    c = make_claimed(q, now=0.0)
    assert q.expire(now=5.0) == []            # jeszcze zyje
    expired = q.expire(now=11.0)
    assert [t["id"] for t in expired] == [c["id"]]
    t = q.get(c["id"])
    assert t["status"] == "open" and t["assignee"] is None
    assert q.expire(now=12.0) == []           # DOKLADNIE raz


def test_blocked_freezes_lease():
    q = TaskQueue(lease_ttl=10.0)
    c = make_claimed(q, now=0.0)
    q.block(c["id"], "beta", 1, "c-blk", expected_version=c["version"], now=1.0)
    assert q.expire(now=100.0) == []          # zamrozony nie wygasa
    t = q.get(c["id"])
    q.unblock(t["id"], "beta", 1, "c-unblk", expected_version=t["version"], now=100.0)
    assert q.get(c["id"])["status"] == "claimed"
    assert q.expire(now=105.0) == []          # swiezy lease po odmrozeniu
    assert len(q.expire(now=111.0)) == 1      # i normalnie wygasa


def test_review_cycle_changes_requested_keeps_assignee():
    q = TaskQueue(lease_ttl=10.0)
    c = make_claimed(q, now=0.0)
    q.to_review(c["id"], "beta", 1, "c-rev", expected_version=c["version"], now=1.0)
    assert q.expire(now=100.0) == []          # w review lease nie tyka
    t = q.get(c["id"])
    q.request_changes(t["id"], "c-chg", expected_version=t["version"], now=100.0)
    t = q.get(c["id"])
    assert t["status"] == "claimed" and t["assignee"] == "beta"
    t2 = q.get(c["id"])
    q.to_review(t2["id"], "beta", 1, "c-rev2", expected_version=t2["version"], now=101.0)
    t3 = q.get(c["id"])
    q.done(t3["id"], "beta", 1, "c-done", expected_version=t3["version"], now=102.0)
    assert q.get(c["id"])["status"] == "done"


def test_done_after_changes_not_deduped_with_first_done():
    # rozne command_id => rozne komendy; dedup nie sklei ich w jedno
    q = TaskQueue()
    c = make_claimed(q, now=0.0)
    q.to_review(c["id"], "beta", 1, "r1", expected_version=2, now=0.0)
    q.request_changes(c["id"], "chg", expected_version=3, now=0.0)
    q.to_review(c["id"], "beta", 1, "r2", expected_version=4, now=0.0)
    q.done(c["id"], "beta", 1, "d2", expected_version=5, now=0.0)
    assert q.get(c["id"])["status"] == "done"


def test_wip_limit_gates_offerable():
    q = TaskQueue(wip_limit=1)
    t1 = q.add(CARD, "a1", now=0.0)
    q.add(dict(CARD, goal="drugi"), "a2", now=0.0)
    assert q.offerable()["id"] == t1["id"]
    q.claim(t1["id"], "beta", 1, "cl", expected_version=1, now=0.0)
    assert q.offerable() is None              # WIP pelny
    # blocked parkuje POZA limitem WIP
    t1c = q.get(t1["id"])
    q.block(t1["id"], "beta", 1, "blk", expected_version=t1c["version"], now=0.0)
    assert q.offerable() is not None


def test_dump_restore_roundtrip():
    q = TaskQueue(lease_ttl=10.0)
    c = make_claimed(q, now=0.0)
    q2 = TaskQueue.restore(q.dump(), lease_ttl=10.0)
    assert q2.get(c["id"])["status"] == "claimed"
    assert len(q2.expire(now=11.0)) == 1      # lease przezyl podroz
```

- [ ] **Step 2: Uruchom — nowe mają polec**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_tasks.py -v`
Expected: 4 passed (stare), 6 failed (`AttributeError`)

- [ ] **Step 3: Implementacja — dopisz do klasy `TaskQueue`**

```python
    # chat/tasks.py — DOPISZ w klasie TaskQueue
    def _mutate(self, task_id, command_id, expected_version, now,
                owner=None, from_status=None, **updates):
        cached = self._dedup_get(command_id, now)
        if cached is not None:
            return cached
        task = self._tasks[task_id]
        self._check(task, expected_version)
        if owner is not None:
            self._check_owner(task, *owner)
        if from_status and task["status"] not in from_status:
            raise Conflict(f"{task_id}: status {task['status']}, oczekiwano {from_status}")
        task.update(version=task["version"] + 1, **updates)
        return self._dedup_put(command_id, copy.deepcopy(task), now)

    def block(self, task_id, nick, generation, command_id, expected_version, now):
        return self._mutate(task_id, command_id, expected_version, now,
                            owner=(nick, generation), from_status=("claimed",),
                            status="blocked", frozen=True)

    def unblock(self, task_id, nick, generation, command_id, expected_version, now):
        return self._mutate(task_id, command_id, expected_version, now,
                            owner=(nick, generation), from_status=("blocked",),
                            status="claimed", frozen=False,
                            lease_until=now + self.lease_ttl)

    def to_review(self, task_id, nick, generation, command_id, expected_version, now):
        return self._mutate(task_id, command_id, expected_version, now,
                            owner=(nick, generation), from_status=("claimed",),
                            status="review", frozen=True)

    def request_changes(self, task_id, command_id, expected_version, now):
        return self._mutate(task_id, command_id, expected_version, now,
                            from_status=("review",),
                            status="claimed", frozen=False,
                            lease_until=now + self.lease_ttl)

    def done(self, task_id, nick, generation, command_id, expected_version, now):
        return self._mutate(task_id, command_id, expected_version, now,
                            owner=(nick, generation), from_status=("review",),
                            status="done", frozen=False, lease_until=None)

    def expire(self, now):
        reopened = []
        for task in self._tasks.values():
            if (task["status"] == "claimed" and not task["frozen"]
                    and task["lease_until"] is not None
                    and task["lease_until"] <= now):
                task.update(status="open", assignee=None, generation=None,
                            lease_until=None, version=task["version"] + 1)
                reopened.append(copy.deepcopy(task))
        return reopened

    def offerable(self):
        wip = sum(1 for t in self._tasks.values()
                  if t["status"] in ("claimed", "review"))
        if wip >= self.wip_limit:
            return None
        for task in self._tasks.values():  # dict zachowuje kolejnosc wstawien
            if task["status"] == "open":
                return copy.deepcopy(task)
        return None

    def dump(self):
        return {"next_id": self._next_id,
                "tasks": copy.deepcopy(list(self._tasks.values()))}

    @classmethod
    def restore(cls, data, **kwargs):
        q = cls(**kwargs)
        q._next_id = data["next_id"]
        q._tasks = {t["id"]: t for t in copy.deepcopy(data["tasks"])}
        return q
```

- [ ] **Step 4: Testy zielone**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_tasks.py -v`
Expected: 10 passed

- [ ] **Step 5: Commit**

```bash
git add chat/tasks.py tests/test_tasks.py
git commit -m "b1: tasks cz.2 — lease/expiry raz, blocked-freeze, cykl review, WIP, dump"
```

---

### Task 6: `chat/server.py` — wiring: hello, backlog/resync, echo po nicku, wzmianki

**Files:**
- Create: `chat/server.py`
- Test: `tests/test_server_integration.py`

**Interfaces:**
- Consumes: `EventLog`, `Registry`, `TaskQueue`, `protocol.*` — sygnatury z Zadań 1–5.
- Produces: `ChatServer(data_dir, tokens, port, wip_limit=3, lease_ttl=120.0, offer_timeout=5.0)` z async `start()` / `stop()`; uruchamialny `python -m chat.server` (env: `CHAT_PORT`, `CHAT_DATA`, `CHAT_TOKENS` = ścieżka do JSON `{nick: token}`).
- Zachowanie: klient po połączeniu wysyła `hello` (`nick`, `instance_id`, `token`, `last_seq`, opcjonalnie `role`). Serwer: auth → `Registry.hello` → odpowiedź `ok` z `generation` + (backlog: `events_after(last_seq)`; None → `resync_required` ze snapshotem stanu kolejki i `snapshot_seq`). Ramki `chat`: trwały zapis PRZED publikacją; dostarczenie TYLKO do socketów nicków wspomnianych (`@nick`) lub wszystkich przy `@all` — nigdy do socketów logicznego NADAWCY (echo po nicku); klienci z `role=="human"` dostają WSZYSTKO na żywo. `fyi`: tylko zapis do loga. Błędna ramka → `error` z opisem.

- [ ] **Step 1: Napisz failing testy integracyjne**

```python
# tests/test_server_integration.py
import asyncio
import json
import pytest
import websockets

from chat.server import ChatServer

TOKENS = {"alfa": "ta", "beta": "tb", "emil": "te", "gamma": "tg"}
PORT = 8891


@pytest.fixture()
def srv(tmp_path):
    async def _run(coro):
        server = ChatServer(data_dir=tmp_path, tokens=TOKENS, port=PORT,
                            lease_ttl=5.0, offer_timeout=0.3)
        await server.start()
        try:
            return await asyncio.wait_for(coro(server), timeout=10)
        finally:
            await server.stop()
    return _run


async def hello(nick, token, instance="i1", last_seq=0, role="agent"):
    ws = await websockets.connect(f"ws://localhost:{PORT}")
    await ws.send(json.dumps({"type": "hello", "from": nick, "ts": 0.0,
                              "instance_id": instance, "token": token,
                              "last_seq": last_seq, "role": role}))
    reply = json.loads(await ws.recv())
    return ws, reply


async def recv(ws, timeout=2.0):
    return json.loads(await asyncio.wait_for(ws.recv(), timeout))


def test_hello_auth_and_generation(srv):
    async def scenario(server):
        ws, reply = await hello("alfa", "ta")
        assert reply["type"] == "ok" and reply["generation"] == 1
        bad = await websockets.connect(f"ws://localhost:{PORT}")
        await bad.send(json.dumps({"type": "hello", "from": "alfa", "ts": 0.0,
                                   "instance_id": "x", "token": "ZLY",
                                   "last_seq": 0}))
        err = json.loads(await bad.recv())
        assert err["type"] == "error"
        await ws.close()
    asyncio.run(srv(scenario))


def test_mention_routing_and_echo_suppression(srv):
    async def scenario(server):
        a1, _ = await hello("alfa", "ta")            # dwa sockety alfy —
        a2, _ = await hello("alfa", "ta")            # zaden nie dostanie echa
        b, _ = await hello("beta", "tb")
        g, _ = await hello("gamma", "tg")
        await a1.send(json.dumps({"type": "chat", "from": "alfa", "ts": 1.0,
                                  "text": "@beta patrz"}))
        got = await recv(b)
        assert got["text"] == "@beta patrz" and got["seq"] >= 1
        for ws in (a1, a2, g):                        # gamma niewspomniana
            with pytest.raises(asyncio.TimeoutError):
                await recv(ws, timeout=0.4)
        for ws in (a1, a2, b, g):
            await ws.close()
    asyncio.run(srv(scenario))


def test_human_gets_everything_live(srv):
    async def scenario(server):
        h, _ = await hello("emil", "te", role="human")
        a, _ = await hello("alfa", "ta")
        await a.send(json.dumps({"type": "chat", "from": "alfa", "ts": 1.0,
                                 "text": "bez wzmianki"}))
        got = await recv(h)
        assert got["text"] == "bez wzmianki"
        await h.close(); await a.close()
    asyncio.run(srv(scenario))


def test_reconnect_resumes_from_last_seq(srv):
    async def scenario(server):
        a, _ = await hello("alfa", "ta")
        await a.send(json.dumps({"type": "chat", "from": "alfa", "ts": 1.0,
                                 "text": "@beta jeden"}))
        await a.send(json.dumps({"type": "chat", "from": "alfa", "ts": 2.0,
                                 "text": "@beta dwa"}))
        await asyncio.sleep(0.2)                      # niech serwer zapisze
        b, reply = await hello("beta", "tb", last_seq=0)
        texts = [f["text"] for f in reply["backlog"] if f["type"] == "chat"]
        assert texts == ["@beta jeden", "@beta dwa"]
        last = reply["backlog"][-1]["seq"]
        b2, reply2 = await hello("beta", "tb", last_seq=last)
        assert reply2["backlog"] == []                # nic nowego
        for ws in (a, b, b2):
            await ws.close()
    asyncio.run(srv(scenario))


def test_too_old_cursor_gets_resync_required(srv):
    async def scenario(server):
        a, _ = await hello("alfa", "ta")
        for i in range(3):
            await a.send(json.dumps({"type": "chat", "from": "alfa",
                                     "ts": float(i), "text": f"@beta {i}"}))
        await asyncio.sleep(0.2)
        server.snapshot()                              # kompakcja logu
        b, reply = await hello("beta", "tb", last_seq=1)  # kursor sprzed snapshotu
        assert reply["type"] == "resync_required"
        assert reply["snapshot_seq"] >= 3
        assert "state" in reply                        # spojny snapshot stanu
        await a.close(); await b.close()
    asyncio.run(srv(scenario))
```

- [ ] **Step 2: Uruchom — mają polec**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_server_integration.py -v`
Expected: FAIL (`ImportError`)

- [ ] **Step 3: Implementacja**

```python
# chat/server.py
"""Serwer czatu agentow: hello/auth, backlog/resync, echo po nicku,
wzmianki, oferty taskow. Jedyny modul z asyncio/websockets."""
import asyncio
import json
import os
import time
from pathlib import Path

import websockets

from . import protocol
from .identity import AuthError, Registry
from .store import EventLog
from .tasks import Conflict, StaleGeneration, TaskQueue


class ChatServer:
    def __init__(self, data_dir, tokens, port, wip_limit=3,
                 lease_ttl=120.0, offer_timeout=5.0):
        self.port = port
        self.offer_timeout = offer_timeout
        self.log = EventLog(Path(data_dir))
        self.registry = Registry(tokens)
        snap = self.log.load_snapshot()
        if snap:
            self.queue = TaskQueue.restore(snap[0].get("queue", {"next_id": 0, "tasks": []}),
                                           wip_limit=wip_limit, lease_ttl=lease_ttl)
        else:
            self.queue = TaskQueue(wip_limit=wip_limit, lease_ttl=lease_ttl)
        self.conns = {}    # nick -> set[ws]
        self.roles = {}    # nick -> role
        self.idle = []     # nicki wyrobnic zglaszajacych idle (round-robin)
        self._offering = None
        self._server = None
        self._expiry_task = None

    # -- infrastruktura ----------------------------------------------------
    async def start(self):
        self._server = await websockets.serve(self._handler, "localhost", self.port)
        self._expiry_task = asyncio.ensure_future(self._expiry_loop())

    async def stop(self):
        self._expiry_task.cancel()
        self._server.close()
        await self._server.wait_closed()

    def snapshot(self):
        self.log.save_snapshot({"queue": self.queue.dump()})

    async def _expiry_loop(self):
        while True:
            await asyncio.sleep(1.0)
            for task in self.queue.expire(time.time()):
                self.log.append(protocol.make_frame(
                    "fyi", "server", time.time(),
                    text=f"lease wygasl, task {task['id']} wraca do open"))
                self._trigger_offer()

    # -- dostarczanie ------------------------------------------------------
    async def _send(self, nick, payload):
        data = json.dumps(payload)
        for ws in list(self.conns.get(nick, ())):
            try:
                await ws.send(data)
            except websockets.exceptions.ConnectionClosed:
                pass

    async def _publish_chat(self, event):
        sender = event["from"]
        mentions = protocol.parse_mentions(event.get("text", ""))
        if "all" in mentions:
            targets = set(self.conns) - {sender}
        else:
            targets = {n for n in mentions if n in self.conns} - {sender}
        targets |= {n for n, r in self.roles.items()
                    if r == "human" and n != sender}
        for nick in targets:
            await self._send(nick, event)

    # -- handler -----------------------------------------------------------
    async def _handler(self, ws):
        nick = None
        try:
            raw = await ws.recv()
            frame = json.loads(raw)
            if frame.get("type") != "hello":
                await ws.send(json.dumps(protocol.make_frame(
                    "error", "server", time.time(), text="pierwsza ramka musi byc hello")))
                return
            try:
                generation = self.registry.hello(
                    frame["from"], frame["instance_id"], frame.get("token"))
            except AuthError as e:
                await ws.send(json.dumps(protocol.make_frame(
                    "error", "server", time.time(), text=str(e))))
                return
            nick = frame["from"]
            self.conns.setdefault(nick, set()).add(ws)
            self.roles[nick] = frame.get("role", "agent")
            backlog = self.log.events_after(frame.get("last_seq", 0))
            if backlog is None:
                self.snapshot()
                reply = protocol.make_frame(
                    "resync_required", "server", time.time(),
                    snapshot_seq=self.log.snapshot_seq,
                    state={"queue": self.queue.dump()}, generation=generation)
            else:
                reply = protocol.make_frame(
                    "ok", "server", time.time(),
                    generation=generation, backlog=backlog,
                    last_seq=self.log.last_seq)
            await ws.send(json.dumps(reply))
            async for raw in ws:
                await self._on_frame(json.loads(raw), nick)
        except (websockets.exceptions.ConnectionClosed, json.JSONDecodeError):
            pass
        finally:
            if nick and ws in self.conns.get(nick, set()):
                self.conns[nick].discard(ws)

    async def _on_frame(self, frame, nick):
        err = protocol.validate(frame)
        if err:
            await self._send(nick, protocol.make_frame(
                "error", "server", time.time(), text=err))
            return
        frame["from"] = nick  # tozsamosc z hello, nie z ramki
        ftype = frame["type"]
        if ftype == "chat":
            seq = self.log.append(frame)   # najpierw trwale, potem publikacja
            frame["seq"] = seq
            await self._publish_chat(frame)
        elif ftype == "fyi":
            self.log.append(frame)
        elif ftype == "status":
            self.log.append(frame)
            if frame.get("state") == "idle" and nick not in self.idle:
                self.idle.append(nick)
                self._trigger_offer()
        elif ftype in ("task_new", "task_claim", "task_done",
                       "task_blocked", "review_changes"):
            await self._on_task_frame(frame, nick)

    async def _on_task_frame(self, frame, nick):
        now = time.time()
        try:
            gen = frame.get("generation")
            if gen is not None and not self.registry.is_current(nick, gen):
                raise StaleGeneration(f"generation {gen} nieaktualna dla {nick}")
            if frame["type"] == "task_new":
                result = self.queue.add(frame["card"], frame["command_id"], now)
                self._trigger_offer()
            elif frame["type"] == "task_claim":
                result = self.queue.claim(frame["task_id"], nick, gen,
                                          frame["command_id"],
                                          frame["expected_task_version"], now)
                if nick in self.idle:
                    self.idle.remove(nick)
            elif frame["type"] == "task_done":
                result = self.queue.to_review(frame["task_id"], nick, gen,
                                              frame["command_id"],
                                              frame["expected_task_version"], now)
            elif frame["type"] == "task_blocked":
                result = self.queue.block(frame["task_id"], nick, gen,
                                          frame["command_id"],
                                          frame["expected_task_version"], now)
            else:  # review_changes (od matki)
                result = self.queue.request_changes(frame["task_id"],
                                                    frame["command_id"],
                                                    frame["expected_task_version"], now)
            self.log.append({**frame, "result_version": result["version"]})
            await self._send(nick, protocol.make_frame(
                "ok", "server", now, command_id=frame["command_id"], task=result))
            if frame["type"] == "review_changes":
                await self._send(result["assignee"], protocol.make_frame(
                    "review_changes", "server", now, task=result))
        except (Conflict, StaleGeneration, KeyError) as e:
            await self._send(nick, protocol.make_frame(
                "error", "server", now,
                command_id=frame.get("command_id"), text=f"{type(e).__name__}: {e}"))

    # -- oferty (round-robin + timeout) -------------------------------------
    def _trigger_offer(self):
        if self._offering is None or self._offering.done():
            self._offering = asyncio.ensure_future(self._offer_loop())

    async def _offer_loop(self):
        while True:
            task = self.queue.offerable()
            if task is None or not self.idle:
                return
            nick = self.idle.pop(0)
            envelope = protocol.make_envelope(
                nick, [protocol.make_frame("task_offer", "server", time.time(),
                                           task=task)],
                self.log.last_seq, self.log.last_seq)
            await self._send(nick, protocol.make_frame(
                "task_offer", "server", time.time(), task=task,
                activation_id=envelope["activation_id"]))
            await asyncio.sleep(self.offer_timeout)
            fresh = self.queue.get(task["id"])
            if fresh["status"] == "open":       # nie wzial — oferta dla innego
                self.idle.append(nick)          # na koniec kolejki
                if len(self.idle) == 1:
                    return                      # nikt inny nie czeka


def main():
    tokens_path = os.environ.get("CHAT_TOKENS", "tokens.json")
    tokens = json.loads(Path(tokens_path).read_text())
    server = ChatServer(
        data_dir=os.environ.get("CHAT_DATA", "./chat-data"),
        tokens=tokens,
        port=int(os.environ.get("CHAT_PORT", 8765)))

    async def run():
        await server.start()
        print(f"chat server on ws://localhost:{server.port}", flush=True)
        await asyncio.Future()

    asyncio.run(run())


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Testy zielone**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_server_integration.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add chat/server.py tests/test_server_integration.py
git commit -m "b1: server — hello/auth, backlog/resync, echo po nicku, wzmianki, humans-live"
```

---

### Task 7: oferty round-robin + `send.py` v2 + aktualizacja starych testów

**Files:**
- Modify: `send.py` (hello + plik sesji), `test_chat.py` (nowa semantyka), `tests/test_server_integration.py` (testy ofert)
- Create: `tokens.example.json`

**Interfaces:**
- Consumes: `ChatServer` (Zadanie 6), protokół hello.
- Produces: `send.py` z env `CHAT_PORT`, `CHAT_TOKEN`, `CHAT_NICK`; plik sesji `.chat-session.json` (`{"instance_id": "..."}`, tworzony przy pierwszym użyciu, `uuid4`); tryby `send.py <nick> "tekst"` i `send.py --listen` wysyłają hello przed czymkolwiek.

- [ ] **Step 1: Dopisz failing testy ofert do `tests/test_server_integration.py`**

```python
# tests/test_server_integration.py — DOPISZ
async def send_status_idle(ws, nick):
    await ws.send(json.dumps({"type": "status", "from": nick, "ts": 0.0,
                              "state": "idle"}))


def test_task_offer_goes_to_one_idle_worker(srv):
    async def scenario(server):
        b, _ = await hello("beta", "tb")
        g, _ = await hello("gamma", "tg")
        await send_status_idle(b, "beta")
        await send_status_idle(g, "gamma")
        a, _ = await hello("alfa", "ta")
        await a.send(json.dumps({"type": "task_new", "from": "alfa", "ts": 0.0,
                                 "command_id": "n1",
                                 "card": {"goal": "x", "acceptance": "y",
                                          "verify": "true", "files": [],
                                          "head": "h", "brief": "b"}}))
        await recv(a)                                  # ok dla task_new
        offer = await recv(b)                          # TYLKO beta (pierwsza idle)
        assert offer["type"] == "task_offer"
        assert "activation_id" in offer
        with pytest.raises(asyncio.TimeoutError):
            await recv(g, timeout=0.2)                 # gamma spi — no herd
        for ws in (a, b, g):
            await ws.close()
    asyncio.run(srv(scenario))


def test_offer_timeout_moves_to_next_worker(srv):
    async def scenario(server):
        b, _ = await hello("beta", "tb")
        g, _ = await hello("gamma", "tg")
        await send_status_idle(b, "beta")
        await send_status_idle(g, "gamma")
        a, _ = await hello("alfa", "ta")
        await a.send(json.dumps({"type": "task_new", "from": "alfa", "ts": 0.0,
                                 "command_id": "n2",
                                 "card": {"goal": "x", "acceptance": "y",
                                          "verify": "true", "files": [],
                                          "head": "h", "brief": "b"}}))
        await recv(a)
        await recv(b)                                  # beta dostaje oferte...
        offer_g = await recv(g, timeout=2.0)           # ...ignoruje -> gamma
        assert offer_g["type"] == "task_offer"
        for ws in (a, b, g):
            await ws.close()
    asyncio.run(srv(scenario))
```

- [ ] **Step 2: Uruchom — oferty zielone od razu (implementacja z Zadania 6); jeśli któryś polegnie, popraw `_offer_loop` aż przejdą**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/test_server_integration.py -v`
Expected: 7 passed

- [ ] **Step 3: Przepisz `send.py`**

```python
#!/usr/bin/env python3
"""Klient CLI czatu agentow (B1: hello + instance_id).

  python3 send.py <nick> "tekst"   -> hello, jedna ramka chat, wyjscie
  python3 send.py --listen         -> hello, wypisuj ruch az Ctrl+C
Env: CHAT_PORT (8765), CHAT_TOKEN (wymagany), CHAT_NICK (dla --listen).
Plik sesji .chat-session.json trzyma instance_id.
"""
import asyncio
import json
import os
import sys
import uuid
from pathlib import Path

import websockets

URI = f"ws://localhost:{os.environ.get('CHAT_PORT', 8765)}"
SESSION_FILE = Path(__file__).with_name(".chat-session.json")


def instance_id():
    if SESSION_FILE.exists():
        return json.loads(SESSION_FILE.read_text())["instance_id"]
    iid = str(uuid.uuid4())
    SESSION_FILE.write_text(json.dumps({"instance_id": iid}))
    return iid


async def do_hello(ws, nick, role="agent"):
    await ws.send(json.dumps({
        "type": "hello", "from": nick, "ts": 0.0,
        "instance_id": instance_id(),
        "token": os.environ.get("CHAT_TOKEN", ""),
        "last_seq": 0, "role": role}))
    reply = json.loads(await ws.recv())
    if reply["type"] == "error":
        print(f"hello odrzucone: {reply['text']}", file=sys.stderr)
        sys.exit(1)
    return reply


async def send_once(nick, text):
    async with websockets.connect(URI) as ws:
        await do_hello(ws, nick)
        await ws.send(json.dumps({"type": "chat", "from": nick,
                                  "ts": 0.0, "text": text}))


async def listen(nick):
    async with websockets.connect(URI) as ws:
        await do_hello(ws, nick, role="human")
        async for message in ws:
            data = json.loads(message)
            print(f"{data.get('from', '?')}: {data.get('text', json.dumps(data))}",
                  flush=True)


def main():
    args = sys.argv[1:]
    try:
        if args == ["--listen"]:
            asyncio.run(listen(os.environ.get("CHAT_NICK", "listener")))
        elif len(args) == 2:
            asyncio.run(send_once(args[0], args[1]))
        else:
            print('usage: send.py <nick> "tekst"  |  send.py --listen',
                  file=sys.stderr)
            sys.exit(1)
    except KeyboardInterrupt:
        pass
    except OSError as e:
        print(f"blad polaczenia z {URI}: {e}", file=sys.stderr)
        sys.exit(1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Utwórz `tokens.example.json` i zaktualizuj `test_chat.py`**

```json
{"alfa": "zmien-mnie-a", "beta": "zmien-mnie-b", "emil": "zmien-mnie-e"}
```

W `test_chat.py`: fixture `chat_server` odpala teraz `python -m chat.server` z env `CHAT_TOKENS` wskazującym tymczasowy plik tokenów (zapisz w fixture do `tmp_path_factory.mktemp("tok") / "tokens.json"` zawartość `{"alfa": "ta", "beta": "tb", "s1": "t1", "s2": "t2"}`), `CHAT_DATA` na katalog tymczasowy oraz `CHAT_PORT=8799`. Każdy klient w testach wysyła najpierw hello (użyj helpera jak w `tests/test_server_integration.py`, z `CHAT_TOKEN` per nick). Test `test_sender_does_not_receive_own_frame` zmienia sens: dwa sockety TEGO SAMEGO nicka ("alfa") — żaden nie dostaje ramki (echo po nicku); odbiorca musi być wspomniany: tekst `"@beta echo test"`. Test `test_broadcast_reaches_other_clients` aktualizuje tekst na `"@all czesc wszystkim"` (wzmianka @all = dawny broadcast). `send.py`-testy dostają env `CHAT_TOKEN`.

- [ ] **Step 5: Wszystkie testy zielone**

Run: `uv run --quiet --with pytest --with websockets python -m pytest tests/ test_chat.py -v`
Expected: wszystkie passed (10 tasks + 7 integration + 4 protocol + 5 store + 3 identity + 5 legacy)

- [ ] **Step 6: Commit**

```bash
git add send.py test_chat.py tokens.example.json tests/
git commit -m "b1: send.py v2 z hello/instance_id, oferty round-robin, legacy testy na nowej semantyce"
```

---

## Mapowanie bramki akceptacji B1 → testy

| Inwariant (spec, commit cfd9577) | Test |
|---|---|
| stare generation odrzucone | `test_stale_generation_rejected`, `test_takeover_bumps_generation_and_invalidates_old` |
| command_id deduplikowane | `test_command_id_dedup_returns_same_result_no_double_mutation` |
| błędny expected_task_version → konflikt bez mutacji | `test_cas_conflict_without_mutation` |
| reconnect odtwarza od last_applied_seq | `test_reconnect_resumes_from_last_seq` |
| zbyt stary kursor → resync_required + spójny snapshot | `test_too_old_cursor_gets_resync_required`, `test_snapshot_then_old_cursor_requires_resync` |
| duplikat activation_id nie uruchamia drugiej pracy | `test_envelope_deterministic_activation_id` (deterministyczne id = strona serwera; dedup wykonuje adapter w B2) |
| task_offer timeout → następna wyrobnica | `test_offer_timeout_moves_to_next_worker`, `test_task_offer_goes_to_one_idle_worker` |
| wygaśnięcie lease przywraca task dokładnie raz | `test_lease_expiry_reopens_exactly_once`, `test_blocked_freezes_lease` |

## ANEKS (2026-07-22, po konsensusie tercetu + @Emil): role i config

Spec rozszerzony o sekcję "Role i autoryzacja — config-first". Wpływ na plan:

- **Task 3 (identity) rozszerzony**: `Registry` przyjmuje enrollment-kody
  jako verifiery scrypt (`hashlib.scrypt`, stdlib) z max_uses/expiry;
  `enroll(role, secret, nick, instance_id) -> (agent_id, token)`;
  `hello(nick, agent_id, token, instance_id) -> generation` (token zamiast
  porównania z tokens-dict). Testy dodatkowo: zły kod enrollment odrzucony,
  max_uses egzekwowane, token agenta działa po rotacji kodu roli.
- **Task 6 (server) rozszerzony**: macierz uprawnień jako słownik
  `PERMISSIONS = {"admin": {...}, "moderator": {...}, "worker": {...}}`;
  walidacja roli per komenda PRZED wywołaniem kolejki (rola z rejestru,
  nigdy z ramki); `rules` + `rules_hash` z `hub.toml` (sekcja `[hub]
  rules = "..."`) dołączane do odpowiedzi na hello. Testy dodatkowo:
  worker nie może `task_new` ani `review_changes` (error, bez mutacji),
  moderator może `review_changes`, hello niesie rules i rules_hash.
- **Task 7**: `tokens.example.json` zastąpione przez `hub.toml.example`
  (kody-verifiery + rules); `hub.toml` w `.gitignore`. `send.py` czyta
  token agenta z pliku 0600 (`.chat-agent.json`: agent_id+token), sekret
  enrollment TYLKO przez stdin/env `CHAT_ENROLL_SECRET` — nigdy argv.
- Launcher `cowork_agents_4_all` (proces huba dla człowieka) i pełne TUI —
  bez zmian, B4; rules_changed przy reload configu — B2.

## Poza zakresem B1 (przypomnienie)

Skill statekmatka (B2), adapter Codex `chat wait` (B2), tailscale (B3), TUI (B4). Heartbeat-proces wyrobnicy z rodzic-checkiem — B2 (klient); serwer w B1 wystawia tylko `heartbeat` przez ramkę `status` z `task_id` (dopisać w B2, kolejka już to umie przez `TaskQueue.heartbeat`).
